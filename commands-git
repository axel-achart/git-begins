// Cloner un repository (terminal Git)
$ git clone <repository-url>

// Les 3 commandes pour Push (terminal Git)
$ git add .
$ git commit -m "le message"
$ git push -u origin main

// Commande pour Pull (terminal Git)
$ git pull
